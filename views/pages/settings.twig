{% extends 'layout.twig' %}

{% block content %}
    <div class="starter-template">
        <h1>{{ 'settings'|trans }}</h1>
    </div>
    <div class="row">
        {% include 'partial/settings/rerun.form.html.twig' %}
    </div>
    <div class="row">
        {% include 'partial/settings/technical_informations.html.twig' %}
    </div>
    <div class="row">
        {% include 'partial/settings/settings.form.html.twig' %}
    </div>
    <div class="row">
        {% include 'partial/settings/update.html.twig' %}
    </div>
{% endblock %}